% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mi.R
\name{suest_mi}
\alias{suest_mi}
\alias{coef.suest_mi}
\alias{vcov.suest_mi}
\alias{nobs.suest_mi}
\alias{summary.suest_mi}
\alias{print.suest_mi}
\title{Pool SUEST systems across multiple imputations}
\usage{
suest_mi(fits)

\method{coef}{suest_mi}(object, ...)

\method{vcov}{suest_mi}(object, ...)

\method{nobs}{suest_mi}(object, ...)

\method{summary}{suest_mi}(object, conf.level = 0.95, ...)

\method{print}{suest_mi}(x, ...)
}
\arguments{
\item{fits}{A list containing at least two compatible \code{"suest_model"}
objects, one per imputation; a \code{"mira"} object containing those fits; or
a result list returned by \code{mitools::with.imputationList()}.}

\item{object, x}{A \code{"suest_mi"} object.}

\item{...}{Additional arguments; currently ignored.}

\item{conf.level}{Confidence level for \code{summary.suest_mi()}.}
}
\value{
A \code{"suest_mi"} object containing pooled coefficients, total,
within-imputation, and between-imputation covariance matrices, Rubin
large-sample degrees of freedom, and the original SUEST fits.
}
\description{
\code{suest_mi()} pools complete joint SUEST coefficient vectors and covariance
matrices using Rubin's rules. Supply either a list whose elements are
compatible \code{suest()} results fitted in separate imputed datasets, or the
\code{mice::mira} object returned by \code{with()} when that expression fits a SUEST
system. Result lists created by \code{mitools::with.imputationList()} are also
accepted directly.
}
\details{
The pooled covariance is
\code{Ubar + (1 + 1 / m) B}, where \code{Ubar} is the mean within-imputation
joint covariance, \code{B} is the between-imputation covariance of the complete
joint coefficient vector, and \code{m} is the number of imputations. Because
each within-imputation input is a complete SUEST system, cross-model covariance
is retained in both components.

A \code{"mira"} object from \code{mice::with()} is read through its public
\code{analyses} component. The list returned by \code{mice::getfit()} is accepted as
well. The \code{mice} package is not required when pooling an ordinary list.

Results from \code{mitools::with.imputationList()} use the same list-based route,
with their originating call retained in the returned object. Legacy
\code{"imputationResultList"} wrappers are accepted as well. The \code{mitools}
package is not required for an already-created result list.

This initial interface supports coefficients, covariance matrices, summaries,
and coefficient-level hypotheses. Predictions, slopes, and comparisons must
be estimated within each imputation and pooled as estimands; they are not yet
implemented for \code{"suest_mi"} objects.
}
