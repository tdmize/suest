SUEST 0.1.4 RELEASE CANDIDATE

This release candidate includes the certified survey-binary increment together
with the model adapters, pweights, clustering, panel, GEE, multiple-imputation,
and numerical acceptance work documented in NEWS.md and tools/STATA-PARITY.md.

COPY

Copy everything inside this archive's top-level suest folder into:

C:\Users\trent\suest

Replace existing files.

RELEASE VALIDATION

Restart R, then run from the repository root:

setwd("C:/Users/trent/suest")
devtools::test()
source("tools/acceptance-tests/run_all_tests.R")
source("tools/preflight.R")

The certified 2026-09-09 run passed the focused survey tests, full unit suite,
144-case numerical acceptance suite, vignette build, clean install/load, and
R CMD check --no-manual with no errors, warnings, or notes. Re-run the release
gates after any source change made beyond release metadata or documentation.
