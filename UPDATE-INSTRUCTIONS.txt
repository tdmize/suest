SUEST 0.1.6.9000 DEVELOPMENT SOURCE - COPY INTO YOUR EXISTING REPOSITORY

This ZIP contains the complete source folder layout. There is no enclosing
suest directory and no .git directory inside the ZIP. The 0.1.6 release is
already published; release instructions in older handoffs are historical.

1. In Git Bash, inspect your existing repository before copying:

   cd /c/Users/tmize/suest
   git status --short
   git switch main
   git pull --ff-only origin main

   Preserve local edits and reconcile any failed pull before replacing files.

2. Extract the ZIP and copy ALL CONTENTS of the extracted folder into
   C:\Users\tmize\suest. Merge directories and replace corresponding files.
   DESCRIPTION and R should sit directly inside the existing suest folder;
   keep its .git directory.

3. Inspect the result with `git status --short` and `git diff`. The ZIP does
   not push to GitHub or make a new release. GitHub publication is a separate
   step after review and explicit authorization.

See NEWS.md and tools/STABILITY-USABILITY-20260925.md for this development
pass. The raw Stata GSEM discrepancy remains documented in
tools/RELEASE-0.1.6.md and the linked validation reports.
